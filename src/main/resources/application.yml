server:
  port: 8080
spring:
   datasource:
     url: jdbc:postgresql://localhost:5432/bank_db
     username: amine
     password: amine
     driver-class-name: org.postgresql.Driver
   jpa:
     hibernate:
       ddl-auto: update
     show-sql: true
     properties:
       hibernate:
         format_sql: true
     database: postgresql
     database-platform: org.hibernate.dialect.PostgreSQL.Dialect

